# SMTP / Email sending (required for /api/send-quiz-email)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
# Set to 1 for implicit TLS (usually port 465). Otherwise omit / leave blank.
SMTP_SECURE=
PARTICIPANT_ID_SALT=your-participant-id-salt

# Must be a verified sender on your SMTP provider / company domain
# Example: FutureNet <hello@futurenet.com>
EMAIL_FROM=

# Used inside the email content for deep-linking back to the quiz
# Example: https://futurenet-demo.netlify.app
NEXT_PUBLIC_SITE_URL=
